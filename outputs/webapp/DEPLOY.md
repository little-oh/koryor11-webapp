# Deploy

This webapp is ready to run as a Node server.

## Run locally

```bash
cd outputs/webapp
npm start
```

## Environment

- `PORT`: optional, default `8787`
- `HOST`: optional, default `0.0.0.0`

## Notes

- Upload up to 10 source files at once.
- Customer list can be `.xlsx` or `.csv`.
